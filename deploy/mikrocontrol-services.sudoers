# Allow the web service to restart only the listed application services.
# Do not add shells, wildcards or arbitrary systemctl commands here.
www-data ALL=(root) NOPASSWD: /bin/systemctl restart mikrocontrol, /bin/systemctl restart nginx, /bin/systemctl restart postgresql
